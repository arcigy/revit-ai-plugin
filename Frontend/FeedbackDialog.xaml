<Window x:Class="RevitAIPlugin.FeedbackDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Feedback" Height="200" Width="350" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Content="Was the action executed correctly?" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14"/>

        <TextBox Name="txtNote" Grid.Row="1" Height="30" Margin="0,10,0,10" Text="Optional note..." Foreground="Gray"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Name="BtnYes" Content="✅ Yes" Click="BtnYes_Click" Width="80" Margin="5"/>
            <Button Name="BtnNo" Content="❌ No" Click="BtnNo_Click" Width="80" Margin="5"/>
            <!-- Add Note button is redundant if we show TextBox directly, but kept for matching logic -->
            <Button Name="BtnAddNote" Content="📝 Add Note" Click="BtnAddNote_Click" Width="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
